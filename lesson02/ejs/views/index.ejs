<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/style.css" />
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <p>Here are our intrepid heroes:</p>
    <ul>
        <% heroes.forEach((hero) => {%>
            <li> <%= hero.name %>
            <% if (hero.origin) { %>
                of <%= hero.origin %>
            <% } %>
            </li>
        <% }) %>
    </ul>
</body>
</html>
